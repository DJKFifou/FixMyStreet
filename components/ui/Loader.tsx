const Loader = () => (
  <div
  className="absolute top-0 left-0 flex items-center justify-center w-full h-full pointer-events-none">
    <i className="material-symbols-outlined text-6xl! animate-spin">autorenew</i>
  </div>
);

export default Loader;
